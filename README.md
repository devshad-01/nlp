source /home/shad/nlp-env/bin/activateff 

Lecture3 tasks
1.preprocess the data, creating versions of the labelled excel senteces to clean one in csv format. we could do it step by step and have multiple csv files for each step of the preprocessing,

suh that the end of it output is a csv showing row list of words based on whow youve preprocessed the text

2.create the count vecrotizer version of processed document then the tf idf 
3.create the count vectorizer version of unprocessed document then the tf idf

Check your vocabulary 

